# Modern Robotics 学习协议

## 技能协作

每次本书学习同时采用以下三个 skill；本协议定义它们在本课程中的协作方式。

1. **`textbook-study-companion`**：会话开始先读取 `progress.md`、`sources.md`、本协议、当前章节的笔记、QA 与附件；负责恢复进度、维护章节文件配对、状态标记和教材图附件。
2. **`course-study-tutor`**：以本地 PDF 为事实来源，按当前章节顺序讲解；负责公式、术语、图示和完整章节笔记。
3. **`learn-anything`**：在已经展示材料与必要支架后，一次提出一个主动回忆、解释或应用问题；根据回答缩小提示或加深练习，不把教材阅读变成机械问答。

开始新会话时，先恢复上述课程文件，再从 `progress.md` 所示的当前小节继续；不默认从头复述，也不擅自改变项目层面的学习路线。

## 每次学习单元

0. **进度—教材核对（硬性）**：在提问、讲解或写笔记前，先对照 `progress.md`、当前章节笔记/QA 与教材原文，确认当前处于哪一节、哪一个例题或哪一张图；列出已覆盖内容和本次唯一的下一项。检查当前项的正文、公式、图示和 QA 是否完整，未完成前不得跳到后续例题或小节，也不得凭记忆补写教材内容。

1. **回顾**：用一个短问题复现上次的一个核心概念。
2. **读原文**：以本地 PDF 的当前小节和图为准，先给出本节知识点清单。
3. **先看结构**：首次出现教材图、机构、装置或陌生符号时，先截取对应原图或局部图，保存到 `attachments/chXX/`，并嵌入章节笔记与 QA；说明部件、固定部分、连接关系与允许运动后，才出依赖该结构的计数或推导题。
4. **讲解**：从物理直觉到公式；每个关键公式都说明用途、符号、直觉和后续连接。
5. **应用**：做一个具体机器人、几何或数值例子；需要时才写小段代码验证。
6. **主动练习**：先由学习者预测、解释或推导；卡住时给最小提示，不把练习变成猜谜。
7. **记录**：更新章节笔记、学习状态和真实的困惑点。

## 小节状态与互动记录

- 当前正在学习的小节可在标题后标记“（进行中）”。
- 学完后直接删除“（进行中）”；不添加“（已完成）”。
- 每章在 `notes/chXX/` 中保存一对同名文件：章节笔记 `chXX-<topic>.md` 与教师侧问答记录 `chXX-<topic>-qa.md`；相应教材图保存在 `attachments/chXX/` 并嵌入这对文件。
- QA 文件仅记录题目、追问、参考答案、教学意图和下一步，不记录学习者的原始回答。
- 问题会作为后续自行复习的材料保留；不另设掌握等级或复习队列。
- 完成一个小节后，在 `progress.md` 更新当前章节、当前小节与下一个学习动作。

## 难度与背景

- 默认从机器人零基础开始，不默认了解坐标系、刚体、关节、运动学或控制。
- 线性代数、微积分和 Python 在教材首次需要时即时补充，不预先堆砌前置课程。
- 强化学习连接只作为理解与应用的延伸：构型 $q$、速度 $\dot q$、动作和动力学与 MDP/RL 的关系会明确标注，不与教材原意混淆。
- 专有术语首次出现时统一写成“中文（English）”形式，例如笛卡尔积（Cartesian product）、构型空间（configuration space, C-space）；后续保持同一译名和英文写法，必要时才使用缩写。

## 笔记格式

- 行内公式使用 `$...$`，例如 `$\theta$`。
- 独立公式使用 `$$...$$`。
- 笔记不复制教材原文，只记录推导、直觉、例子和易错点。
